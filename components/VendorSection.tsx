import { ScrollView, StyleSheet, Text } from 'react-native'
import React from 'react'

const VendorSection = () => {
  return (
    <ScrollView>
      <Text>VendorSection</Text>
    </ScrollView>
  )
}

export default VendorSection

const styles = StyleSheet.create({
    
})